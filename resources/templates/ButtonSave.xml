<c:template name="button-save">
    <p:param name="buttons" type="source"/>
    <p:body>
        <div class="buttonBar right">
          <c:button class="btn-primary ActionSave" action="submit" label="$BUTTON_SAVE"/>
          {@buttons}
        </div>
    </p:body>
</c:template>
